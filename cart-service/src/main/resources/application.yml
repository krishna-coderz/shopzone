server:
  port: 8083

spring:
  application:
    name: cart-service

  datasource:
    url: jdbc:h2:mem:cartdb

  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
